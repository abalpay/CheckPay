NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional override for Next.js API proxy target
FASTAPI_RECONCILE_URL="http://localhost:8000/api/reconcile"

# Direct reconcile URL for client uploads (bypasses Vercel body size limit)
# If omitted, falls back to /api/reconcile proxy
NEXT_PUBLIC_RECONCILE_URL="http://localhost:8000/api/reconcile"
